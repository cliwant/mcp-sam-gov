{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.cliwant/mcp-sam-gov",
  "description": "Keyless-first MCP: 152 tools for US federal + state/local (SLED) contracting, spending & regulation",
  "repository": {
    "url": "https://github.com/cliwant/mcp-sam-gov",
    "source": "github"
  },
  "version": "1.16.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@cliwant/mcp-sam-gov",
      "version": "1.16.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "SAM_GOV_API_KEY",
          "description": "Optional. SAM.gov key for higher rate limits + archives older than 12 months. Keyless by default.",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "CENSUS_API_KEY",
          "description": "Required only for census_business_patterns (the Census Data API has no keyless tier); every other tool works without it.",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "FRED_API_KEY",
          "description": "Required only for fred_search_series and fred_series_observations (FRED has no keyless tier); every other tool works without it.",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "BEA_API_KEY",
          "description": "Required only for bea_regional_data (the BEA Data API has no keyless tier); every other tool works without it.",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "BLS_API_KEY",
          "description": "Optional free key for BLS labor-statistics tools (raises rate limits).",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "DOL_API_KEY",
          "description": "Required only for dol_get_dataset (DOL's data endpoint has no keyless tier; dol_list_datasets works without it).",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "DATA_GOV_API_KEY",
          "description": "Optional free api.data.gov key; raises DEMO_KEY limits for Regulations.gov, Congress.gov, GovInfo, FAC, the data.gov catalog and GSA per-diem.",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "NVD_API_KEY",
          "description": "Optional free NVD key (raises CVE rate limits).",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "OPENFDA_API_KEY",
          "description": "Optional free openFDA key (raises rate limits).",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "SOCRATA_APP_TOKEN",
          "description": "Optional Socrata app token (raises rate limits on SLED Socrata hosts).",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "COURTLISTENER_API_TOKEN",
          "description": "Optional CourtListener (Free Law Project) token (raises rate limits).",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "LDA_API_KEY",
          "description": "Optional Senate LDA lobbying-disclosure key (raises rate limits).",
          "isSecret": true,
          "isRequired": false
        },
        {
          "name": "EDGAR_USER_AGENT",
          "description": "Optional SEC EDGAR User-Agent string (courtesy identification; a sensible default is used otherwise).",
          "isSecret": false,
          "isRequired": false
        },
        {
          "name": "MCP_SAM_GOV_TOOLSETS",
          "description": "Optional. Comma-separated toolset names to load (e.g. core,sled). Blank or 'all' loads all 152 tools.",
          "isSecret": false,
          "isRequired": false
        }
      ]
    }
  ]
}
