{
  "name": "mcp-sam-gov-worker",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker hosted demo for mcp-sam-gov (HTTP-JSON wrapper around a keyless subset).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "wrangler": "^3.78.0"
  }
}
